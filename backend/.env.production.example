PORT=3000
NODE_ENV=production
JWT_SECRET=replace_with_a_strong_random_secret_at_least_32_chars
FRONTEND_URL=https://your-domain.com
SERVE_FRONTEND=false
FRONTEND_ROOT=../frontend
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vsim_db
DB_USER=postgres
DB_PASSWORD=change_this_password
DB_POOL_MAX=20
DB_POOL_MIN=2
DB_POOL_IDLE_MS=30000
DB_POOL_CONNECT_MS=5000

# Deployment notes:
# - Use HTTPS only in production.
# - Keep this backend as a standalone API on the VPS unless you truly want it to serve frontend static files.
# - Store real secrets outside version control.
# - Use a managed reverse proxy or TLS termination in front of Node.
